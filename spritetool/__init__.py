"""spritetool — recipe-driven sprite sheets for Ledgelings.

The same idea as gig-tools' spritekit, cut down to what a creature needs:
a YAML recipe declares the layout, `template` draws that layout as an image to
paint into, and `pack` turns whatever was painted into the atlas the app loads.
"""
